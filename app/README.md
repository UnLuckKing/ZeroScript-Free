# Desktop application

- `entry.pyw`: the only desktop entrypoint.
- `core/`: Hub runtime and authenticated local control API.
- `ui/`: desktop interface modules.
- `services/`: Memory Vault, recipes and deterministic decision services.

Normal users should launch `ZeroScript One.bat` from the repository root.
