# Roblox Studio bridge

This folder contains the local WebSocket-to-MCP bridge, Roblox Studio MCP launcher and bridge configuration. Normal use starts it automatically through `ZeroScript One.bat`; `bridge/start.bat` is only a diagnostic fallback.
