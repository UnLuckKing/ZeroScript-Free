# Developer tools

- `install_studio_panel.py`: installs the Roblox Studio workspace.
- `build_release.py`: validates and packages a release.
- Matching `.bat` files are Windows shortcuts for these developer operations.
